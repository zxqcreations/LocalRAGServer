# 路线对齐审计 · 2026-08-14 · 第 13 轮（Phase 1 里程碑）

## 本轮完成（Phase 1 最后两个退出标准）

- **SQLite→PostgreSQL 迁移演练**（用户安装 PG 后完成）：建库 → alembic upgrade head → **downgrade base 回滚演练 → 再 upgrade 恢复** → 双库同批数据五项校验（KB 名/文档数/chunk 数/pipeline_version/状态）全部一致；报告 docs/perf/pg-drill-20260814.md；psycopg 入主依赖
- **真实 worker 进程验证**（ADR-002 实机闭环）：发现并修复 kombu filesystem 传输的**收发目录配对陷阱**（生产者写 data_folder_out、消费者读 data_folder_in，默认配置消费者侧还会交换——显式同目录配置才是稳妥形态）；验证通过：worker 进程消费任务链 → job ready/chunk=1/attempt=0
- 模型 MANIFEST（F-17）：三项资产 + GGUF sha256 + gitignore 例外入版本库
- .env.example 补 celery 与 URL 摄取配置

## Phase 1 退出标准终审（quality.md §2）

| 标准 | 状态 |
|---|---|
| 状态机全转移测试 | ✅ 49 对矩阵 |
| 断点续传 / 幂等去重 | ✅ 哈希链 + (kb_id, hash) |
| 覆盖率 ≥80% | ✅ 92.97%（173 测试） |
| 吞吐数据入 docs/perf | ✅ 2850 文档/小时 |
| 免 Docker 部署文档 | ✅ ADR-002 |
| Alembic 可逆 + **SQLite→PG 回滚演练** | ✅ 本轮完成 |
| SSRF 防护测试全绿 | ✅ 25 例 |
| 解析质量抽检 | ✅ 真实数据 |
| pipeline_version | ✅ 已落地 |
| **打标签 v0.2.0** | ⏸️ 待用户提交授权（与自动提交决策一并） |

## 五查结果

1. **范围** ✓ 无蔓延（worker 验证/MANIFEST 均为 Phase 1 内计划项）
2. **技术** ✓ psycopg 为架构 §13 既定 PG 驱动
3. **架构一致性** ✓ 演练逐项对照 quality.md 退出标准
4. **规范** ✓ 173 测试 · 覆盖率 92.97% · ruff/pyright/bandit 全零
5. **交付物** ✓ 演练报告 + worker 验证 + MANIFEST

## 待用户决策

1. **自动提交**（上轮已问）：是否开启每轮 Conventional Commits 自动提交推送
2. **v0.2.0 标签**：Phase 1 DoD 全绿，待提交授权后打标签
3. 安全提醒：PG 密码仅用于本地开发（存放在本机 .env，不入版本库）；建议后续为该库创建专用低权限账号
