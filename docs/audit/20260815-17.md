# 路线对齐审计 · 2026-08-15 · 第 17 轮（RAGAS 评估闭环首测基线）

## 本轮完成

- **RAGAS 全量评测首测**（50/50 条，真实链路：bge-m3 检索 + Qwen3-8B 生成/评判）：
  - 基线：faithfulness **0.872** / answer_relevancy **0.748** /
    context_precision **0.925** / context_recall **0.98**
  - 落盘：docs/perf/ragas-baseline.json + 全量明细（50 条 JSONL）
- **长评测工程化**（实测三轮 20 分钟窗口中断后的对策）：
  - 逐条增量落盘 + `--offset` 续跑（中断零损失）
  - 并行评判（max_workers=3，llama-server 4 slots，单条 ~1min → 3 并行）
  - 分批报告文件（`ragas-eval-<offset>-<ts>.json(l)`）
- **实测修复**：
  - llama-server 默认不支持 /v1/embeddings（501）→ 嵌入改本机 bge-m3
    （与生产检索同源，不引入第二套嵌入）
  - langchain_core 懒依赖 TYPE_CHECKING 模式（CI 无依赖环境模块可导入）
  - 依赖安装经验固化：清华镜像（代理对 pypi 转发故障时）+
    langchain-community<0.4（ragas 0.4.3 硬导入已移除的 vertexai 模块）
- **门禁接入**：quality.md 第 9 项 RAGAS 基线（劣化 >0.05 阻断；自检通过、
  劣化模拟正确拦截）
- **质量信号**：context_precision 0.925 显著高于批 1 早期样本的 0.58——
  与 code 类文档的检索排序波动有关，Phase 5 后续可按 kb_type 分层分析

## 门禁记录

| 门禁 | 结果 |
|---|---|
| pytest（300 例） | ✅ 91%+ 覆盖（本地全绿） |
| ruff / pyright / bandit | ✅ 全零 |
| RAGAS 基线自检 | ✅ 通过（劣化 -0.1 模拟正确拦截） |
| CI（21ba224 推送） | ✅ 全绿（297da9c 推送后 CI 验证中） |

## 下轮输入

1. CI 绿确认 → RAGAS 评测脚本进 GPU nightly job（自托管 runner，手动触发）
2. 爬虫增量摄取设计（Phase 5 第 2 项）
3. 压测基线（scripts/bench_ingest 扩展，Phase 5 第 3 项）
