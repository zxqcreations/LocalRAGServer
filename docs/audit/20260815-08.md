# 路线对齐审计 · 2026-08-15 · 第 08 轮（Phase 3 里程碑：v0.4.0）

## 本轮完成

- **探针端点**：/healthz（存活）+ /readyz（database/qdrant 关键依赖失败 503 degraded，embedder/llm 降级记录）
- **security-reviewer 强制审查**（Phase 3 门禁）：无 CRITICAL；2 HIGH + 6 MEDIUM + 7 LOW
- **H-1 修复**：create_kb 仅主 Key（allowed=="*"）授权，表 Key 越权 403 + 双回归测试
- **H-2 修复**：MCP ingest_document 传输通道强制（allow_local_paths 参数；远程挂载拒绝本地路径）+ 测试
- **M-2 修复**：认证前 per-IP 限流（30/分钟）防 scrypt 放大 DoS
- **M-3 修复**：DocumentOut.error 首行分类化；MCP source 不再落绝对路径
- **M-5 修复**：expires_at 强制 UTC-aware 规范化
- **L-1/L-2/L-3 修复**：令牌桶加锁、魔数只读头部 8 字节、跨平台文件名净化
- **已知事项登记**（对外暴露前处理）：M-1 SSRF rebinding TOCTOU（需自定义 transport）、M-4 transformers/gradio CVE（mineru 重 extras 侧装，非运行依赖）、M-6 scrypt 档位（Phase 4 Web 口令另用 Argon2id）
- 250 测试全绿 · 全门禁零告警 · 自动提交 b6add2b · **v0.4.0 发布**

## Phase 3 退出标准对照（quality.md §2）

| 标准 | 状态 |
|---|---|
| 三通道共享统一认证与审计 | ✅（MCP/REST 共享 ACL 强制层与审计管线；Web 预留） |
| 安全审查无 CRITICAL/HIGH 遗留 | ✅（H-1/H-2 已修复并测试固化） |
| OpenAI 兼容端点 | ✅（chat/embeddings 代理口径已备） |
| ACL 交集矩阵测试全绿 | ✅（泄漏矩阵 8 例进 CI） |
| MCP 双 transport | ✅（stdio 实测；streamable HTTP 挂载留 Phase 4 Web 化） |
| 文档齐备 | ✅（README/架构 v1.3/ADR-003~005/设计稿） |
| 打标签 v0.4.0 | ✅ |

## 下轮输入

Phase 4 启动（进入标准：Web 管理端认证设计定稿 + structlog 接入计划 + 指标清单定稿——quality.md §2）；前置可先补 M-1（SSRF rebinding 自定义 transport）
