# 路线对齐审计 · 2026-08-14 · 第 17 轮

## 本轮完成（Phase 2 前置依赖全部实测）

- **Qdrant 侧**：prefetch/RRF、SparseVector、SparseIndexParams 本机 1.19 全部可用 ✅
- **bge-m3 sparse 探测（决定性）**：本机 sentence-transformers 无 sparse 输出 API →
  **混合检索 sparse 来源改为 Qdrant BM25 文本索引**（零模型变更、本地完全可行）；
  bge-m3 学习稀疏标注为生产 TEI 增强项；两份设计稿已更新
- **环境修复链**（GPU torch 第三次被覆写，本轮根治）：
  1. 定位残留 mineru fast_api 进程锁死 torch pyd 文件 → 清理
  2. 恢复 torch 2.9.1+cu126
  3. 发现双 dist-info 残留（2.13.0 的 METADATA 已损坏，importlib 版本解析为 None）→ 删旧留新
  4. torchvision 版本错配（nms 算子缺失）→ 装匹配的 0.24.1+cu126
  5. **新增守卫测试 tests/test_gpu_env.py**（有 GPU 环境 torch 被覆写即红，CI 自动跳过）
- 177 测试全绿 · ruff/pyright 全零

## Phase 2 进入标准状态

| 标准 | 状态 |
|---|---|
| Phase 1 已发布（v0.2.0） | ⏸️ 待用户提交授权 |
| TEI 部署方案确定 | ✅ 方案 A 完整可行（实测支撑），待用户确认后升 ADR-003 |
| Qdrant 迁移计划获批 | ✅ 草案完成 + 前置依赖全实测通过 |
| 评测集 v1 接入 CI | ✅ 基线门禁已接 |
