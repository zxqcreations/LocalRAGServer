# 路线对齐审计 · 2026-08-14 · 第 08 轮（Phase 1 启动）

## 本轮目标

Phase 1 进入标准推进：MinerU 安装评估 + 状态机契约定稿 + Alembic 迁移体系

## 实际完成

- **MinerU 3.4.5 安装成功**（py3.13 可用，CLI 验证通过；性能评估待下一轮真实 PDF 实测）
- **状态机契约定稿**（docs/design/ingest-state-machine.md）：七状态 + failed 终态、合法转移白名单、阶段幂等、替换语义、对账规则、ingest_jobs 表设计、重试上限 5 次 + DLQ、表驱动测试要求
- **Alembic 迁移体系**：env.py 接入 Settings/SQLModel 元数据；baseline 迁移（9b22eb56d106）；升级/回滚双测试（82 测试全绿）
- **依赖管理治理**：重型运行时依赖（torch/sentence-transformers/mineru）移出 uv extras——本机 uv 0.11.13 不读索引配置，extras 中的 torch 会在 sync 时用 cpu 版覆盖 GPU 版（已在 pyproject 注释 + README 文档化，Phase 1 修复 uv 问题后回归 lock 管理）

## 五查结果

1. **范围** ✓ 按 Phase 1 进入标准顺序推进，无蔓延
2. **技术** ✓ alembic（固定栈外新增，属审计 ARC-008 明确要求的迁移体系，已有 ADR 依据）
3. **架构一致性** ✓ 状态机契约与架构 §5.1 逐字对齐；迁移体系满足 ARC-008 可逆性要求
4. **规范** ✓ 82 测试全绿 · ruff/pyright 全零
5. **交付物** ✓ 契约文档 + 迁移体系 + 测试齐备

## 偏离项与处置

| 偏离 | 处置 |
|---|---|
| 移除 embed extra（原 pyproject 定义） | 环境级原因（uv 索引配置 bug），文档化于 pyproject 注释 + sm75-matrix §4 + README；Phase 1 修复后回归 |

## 下轮输入

#12 续：MinerU 真实 PDF 解析实测（中英/公式/表格样本，记录性能）；#14 Celery 异步管线（任务链 + 状态机实现 + DLQ）；#15 PG 迁移准备
