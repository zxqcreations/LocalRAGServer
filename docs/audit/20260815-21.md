# 路线对齐审计 · 2026-08-15 · 第 21 轮（Phase 5 关键门收尾）

## 本轮完成

- **评测集版本化**（Phase 5 关键门 ②）：DATASET_VERSION=v1；两个 check_baseline
  均校验版本一致（跨版本不可比 → 阻断）；ragas-baseline 补版本字段；测试 +1
- **RAGAS 进 CI**（关键门 ① 机制侧）：gpu-nightly job 增 prepare_eval_kbs +
  ragas_runner --check-baseline 步骤（自托管 runner 手动触发）
- **架构文档 v2.0 草案**（关键门 ⑤）：版本标记升 v2.0-draft；新增 §19 URL
  订阅爬取 / §20 评测闭环 / §21 压测基线（Phase 4/5 落地回填）

## Phase 5 关键门复核（quality.md §2）

| 关键门 | 状态 |
|---|---|
| ① RAGAS 四项进 CI（judge 独立选型防自评偏置） | ⚠️ **部分**：CI 机制已接入；judge 独立选型未做（当前 Qwen3-8B 自评——生成与评判同源，自评偏置风险未消除） |
| ② 评测集版本化 + 污染隔离 | ✅ 版本化本轮完成；污染隔离设计既有 |
| ③ 压测报告与容量估算对照归档 | ✅ 第 20 轮完成 |
| ④ 缺陷清场（无未关闭 CRITICAL/HIGH） | ✅ 双审查多轮闭环 |
| ⑤ 架构文档 v2.0 草案 | ✅ 本轮完成 |

**判定：不打 v0.6.0**——①未全绿，企业级口径不打 tag。judge 独立选型
（第二个评判模型，与生成模型解耦）列为下轮首项。

## 门禁记录

| 门禁 | 结果 |
|---|---|
| pytest（318 例） | ✅ 90.76% 覆盖 |
| ruff / pyright | ✅ 全零 |
| CI | ✅（e68a74a 绿；4f604a8 验证中） |

## 下轮输入

1. judge 独立选型：下载小型评判模型（GGUF，经代理/镜像）→ ragas_runner
   支持独立 judge 模型配置 → 重跑评测更新基线 → Phase 5 关键门 ① 闭环 → v0.6.0
2. 订阅前端面板、conftest 双重加载根治（Phase 5 收尾）
