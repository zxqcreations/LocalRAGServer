# 路线对齐审计 · 2026-08-14 · 第 07 轮

## 本轮目标

Phase 0 收尾：CI 流水线 + README GPU 指引 + 最终门禁复核

## 实际完成

- **CI 三层流水线**（审计 F16/P0-5 落地）：.github/workflows/ci.yml——PR 快速层（ruff/pyright/bandit/pip-audit/pytest≥80%/冒烟，ubuntu+windows 双矩阵、无 GPU 无外联）、检索回归层（评测集校验 + recall@10 报告）、GPU 实测层（自托管 runner，手动触发）
- **README** 补 Windows GPU 实测指引（torch cu126 工作区方案、llama-server 9001、Spike 数据引用）
- **最终门禁复核**：80 测试全绿 · 覆盖率 93.41% · ruff/pyright/bandit/pip-audit 全零 · 冒烟通过 · 评测回归 recall@10=0.880
- 修复 pyright 对 LocalEmbedder 的延迟分析报错（定点忽略 + 说明）

## Phase 0 DoD 对照（quality.md §2）

| 条目 | 状态 |
|---|---|
| /search + /chat 可用（统一信封+错误码） | ✅ |
| 覆盖率 ≥80% CI 强制 | ✅ 93.41% + CI 门禁文件就绪 |
| SM75 兼容矩阵 + 生成后端 ADR 获批 | ✅ sm75-matrix.md + ADR-001 |
| 冒烟脚本全链路可复现 | ✅ |
| 测试隔离基座 | ✅ conftest 三护栏 |
| 最小认证 + 上传防护同批交付 | ✅ |
| 打标签 v0.1.0 | ⏸️ 阻塞于 git 初始提交（见下） |

## 待用户决策

**git 初始提交与 v0.1.0 标签**：仓库已 init（main 分支）但零提交。用户规范要求"提交需明确授权"——请授权后我将创建初始提交（Conventional Commits 格式，含全部 Phase 0 交付物）并打 v0.1.0。

## 下轮输入

获得 git 授权后：初始提交 → v0.1.0 → Phase 1 启动（MinerU 接入、Celery 异步、状态机——进入标准见 quality.md §2）
