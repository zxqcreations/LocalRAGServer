# 路线对齐审计 · 2026-08-15 · 第 01 轮（Phase 2 · parent-child 与路由）

## 本轮完成

- **parent 回填**（core/retrieval/parent.py）：命中子块按文档序号向两侧扩展至 2048 字符；
  实现形态为相邻窗口扩展（不存父块——零向量/存储膨胀，语义等价于父块回填，已记录为设计变体）
- **查询路由**（core/retrieval/router.py）：document/code/web 三型参数化（代码库稀疏优先，
  基于 BM25 对标识符的天然敏感性）；未知类型回退 document
- **生成链接入**：chat 路由用回填上下文组装（架构 §5：子块检索、父块入生成）
- 206 测试全绿（+9）· ruff/pyright 全零 · 基线门禁通过 · 自动提交 9392f53

## 五查结果

1. **范围** ✓ #20 交付物
2. **技术** ✓ 零新增依赖
3. **架构一致性** ✓ §5/§6 回填与路由；窗口扩展为回填实现变体（无 schema 变更，语义等价）
4. **规范** ✓ 206 测试 · 全门禁
5. **交付物** ✓ 两模块 + 集成 + 测试

## 下轮输入

#21 拒答策略三分支（0.249/0.250/0.251 边界 + LLM 零调用断言 + hard-negative）→ #22 检索回归重定基线
