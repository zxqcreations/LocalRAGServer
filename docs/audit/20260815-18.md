# 路线对齐审计 · 2026-08-15 · 第 18 轮（URL 订阅爬取核心）

## 本轮完成

- **URL 订阅爬取核心**（Phase 5 第 2 项，1c4132f）：
  - docs/design/url-crawler.md 定稿（订阅模型/beat 调度/变更检测/退避/版本语义）
  - UrlSubscription 表 + registry 6 方法（含到期扫描：enabled 且游标 ≤ now）
  - fetch_subscription 四态契约（unchanged/ingested/failed/ssrf_blocked）：
    未变化零摄取成本；变更复用 URL 摄取链路（幂等键 kb_id+content_hash）；
    失败记录错误并退避 interval_hours
  - crawl.due beat 任务（10 分钟周期；worker `-B` 启动）
  - SSRF 5 层防护在订阅路径全复用（有测试断言拦截）
- **CI pyright 修复**：可选评测依赖导入补 ignore（CI 无 ragas 环境双平台挂 → 全绿确认）
- **测试基座发现**：pytest 以 `conftest` 与 `tests.conftest` 双重加载 conftest.py——
  `from tests.conftest import X` 与 fixture 使用的类不是同一实例（修改类属性不生效）。
  已在 test_crawl 中规避（经 fixture 对象访问）并注释；test_admin_api 的 API_KEY
  导入为常量无副作用。根治（conftest 单一加载）记为测试基座硬化项。

## 门禁记录

| 门禁 | 结果 |
|---|---|
| pytest（314 例，+14） | ✅ 90.78% 覆盖 |
| ruff / pyright / bandit | ✅ 全零 |
| CI（6224170 修复推送） | ✅ 全绿（1c4132f 验证中） |

## 下轮输入

1. CI 绿确认 → 管理端订阅 API + 前端面板（url-crawler.md 实施步骤 3）
2. worker 启动方式补 `-B`（beat）文档/脚本说明
3. 压测基线（Phase 5 第 3 项）
