# 路线对齐审计 · 2026-08-15 · 第 05 轮（Phase 3 · ACL 强制层）

## 本轮完成

- **ACL 核心**（core/security/acl.py）：scrypt 慢哈希 + 独立盐 + 前缀索引；resolve_allowed_kb_ids（"*" 语义）；require_kb_access 强制点（deny by default + AclDeniedError→403）
- **api_keys 表**：签发（明文仅一次、DB 只存哈希——测试断言）/ 验证（前缀查找 + 过期检查 + last_used_at）/ 吊销即时生效
- **中间件升级**：主 Key（引导凭据，"*"）与表 Key 双通道；请求级 allowed_kbs 注入
- **全路径接线**：search/chat/documents（upload/url/list/get/delete）/kb（get + **列表 ACL 过滤防租户名泄露**）
- **跨 KB 泄漏矩阵 6 例**（越权 403 非 404 / 授权 200 / 摄取删除越权 / chat 越权 / 列表过滤 / 吊销即时 / 主 Key 全权限）
- 232 测试全绿（+17）· ruff/pyright 全零 · 自动提交 e61009f

## 五查结果

1. **范围** ✓ #23 交付物（设计稿 acl-enforcement.md 逐项落地）
2. **技术** ✓ 零新增依赖（hashlib.scrypt 标准库）
3. **架构一致性** ✓ §8.1/§9.2/§12；审计 F-13/F-02/F-18 原文对齐
4. **规范** ✓ 232 测试 · 全门禁
5. **交付物** ✓ 核心 + 表 + 接线 + 矩阵

## 下轮输入

#24 MCP Server（双 transport + 5 工具）→ #25 限流与审计管线 → #26 探针 + security-reviewer 门禁
