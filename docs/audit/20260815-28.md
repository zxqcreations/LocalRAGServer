# 路线对齐审计 · 2026-08-16 · 第 28 轮（门③ SLO 压测闭环）

## 本轮完成

- **Qdrant server（HNSW）实测**（用户授权执行 qdrant.exe，v1.19.0 Windows 官方二进制）：
  - dense 段 194→36.8ms（HNSW 生效）；但端到端仍 717ms 超限
  - **根因深挖**：expand_parents 每查询两次全量加载 KB chunk（万级 ~430ms/查询
    隐藏开销，分解计时未覆盖）
- **修复**（ae17d35）：SearchService 维护 KB 级 chunk 序列缓存（摄取/删除失效），
  expand_parents 复用（chunks_cache 参数，兼容直接调用）
- **复测达标**：万级 P95 **160.1ms**（目标 <500ms；749→160，-79%）；
  分解：dense=36.8 · sparse=84.6 · fuse=116.8 · fetch=1.3
- **部署形态定稿**（docs/runbooks/qdrant-server.md）：server 模式（生产）/
  local 模式（开发）边界与实测数据、启动与备份要求
- bench_search 支持 --server 模式

## 门禁记录

| 门禁 | 结果 |
|---|---|
| pytest（全量） | ✅ 90.61% 覆盖 |
| ruff / pyright / bandit | ✅ 全零 |
| SLO 万级复测 | ✅ P95 160.1ms 达标 |
| CI | ✅（647404b 绿；ae17d35 验证中） |

## Phase 6 门③ 达成

万级 SLO 达标（P95 160.1ms < 500ms）+ 耗时分解归档 + 生产/开发形态边界定稿。
满数据量（10 万级）以线性外推 + 架构 §8.4 口径为验收依据（phase6-plan 风险登记）。

## 下轮输入

门⑤ 发布/回滚 runbook（per-KB feature flag 评估 + 旧模型保留 ≥72h 机制）→
门⑥⑦ 文档化（灰度/迁移，环境受限项）。
