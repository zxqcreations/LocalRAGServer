# 路线对齐审计 · 2026-08-15 · 第 13 轮（Phase 4 · E2E 上线与双审查修复）

## 本轮完成

- **Playwright E2E 上线（4/4 全绿，连续两轮可重复）**：
  - auth-setup 模式：登录 + 强制改密 → storageState 复用（消除一次性初始密码的并行竞争）
  - 断点项目（320px）与主项目依赖隔离；`dependencies: ["setup"]` 保证时序
  - 数据目录清理移入 API 启动包装 `run-e2e-api.mjs`（Playwright webServer 先于 globalSetup 启动，Qdrant 本地模式锁目录导致 EPERM——清理必须早于进程启动）
- **CSRF token 持久化**（修复生产缺陷：页面刷新后所有 POST 403）：
  - api.js：localStorage 持久化 + try/catch 降级（隐私模式 SecurityError）
  - App.vue：/me 自愈（refreshMe 取回当前会话 token）、401/登出清理
- **双审查（code-reviewer + security-reviewer）发现处置**：
  - **H-1**（security HIGH）：CSRF token 与 session_id 同值落盘 → **独立随机 token**（`secrets.token_urlsafe(32)` 签发，会话表持久化，中间件改比对会话记录；Cookie 值冒充 token 被拒——新增契约测试断言）
  - **MEDIUM-1**：多标签页 token 覆盖 → /me 下发 csrf_token，前端自愈
  - **MEDIUM-2/M-1**：Windows 进程树孤儿 → taskkill /T /F + rmSync 可操作报错
  - **MEDIUM-3**：readyz 告警风暴 → 边沿触发（ok→down 告警、恢复 info 回执）
  - LOW：刷新回归测试（reload 后签发 Key）、helpers 绝对路径、nth 选择器、硬编码测试 Key 环境变量化、下载器 scheme 白名单（实测 urlparse 大小写/反斜杠/file:// 变体均拦截或 fail-closed）、B107 误报 nosec
- **bandit 门禁治理**：CI 口径（含 scripts）下 19 条发现归零——smoke.py assert→_check（B101 真实修复）、固定 argv 的 subprocess 依据注释、下载器 scheme 校验

## 门禁记录

| 门禁 | 结果 |
|---|---|
| pytest（276 例，+3 新契约） | ✅ 93.42% 覆盖率 |
| ruff / pyright（core apps scripts eval） | ✅ 零错误 |
| bandit（core apps scripts） | ✅ exit 0 |
| pip-audit | ✅ 无漏洞（3 项本地包跳过） |
| E2E（4 用例 × 2 轮） | ✅ 全绿（含 reload 回归） |
| vite build | ✅ gzip 40.5KB |
| 冒烟脚本 | ✅ 全链路 OK |
| code-reviewer / security-reviewer | ✅ 无 CRITICAL/HIGH 遗留（H-1 已修） |

## 架构注记

- `adminsession` 等管理表由 `Registry.__init__` 的 create_all 管理（未入 alembic 迁移）；H-1 新增列通过启动期幂等自愈补列（create_all 只建表不补列，ALTER ADD COLUMN 带 DEFAULT 在 SQLite/PG 均幂等），有测试覆盖。管理表全面入 alembic 记为 Phase 6 硬化项。
- E2E 数据目录 `.e2e-data`、`.auth/`（含活会话凭证的 storageState）、test-results 已全部 gitignore。

## Phase 4 退出标准对照（quality.md §2）

| 标准 | 状态 |
|---|---|
| Web 管理端独立认证 | ✅ 契约测试 10 例（含独立 token/自愈/越权） |
| 5 模块 Playwright E2E | ✅ 4/4 绿 + 断点 320px + 可重复 |
| trace_id 全链路验证 | ✅ 中间件 + 测试 |
| 告警规则集上线 | ✅ 边沿触发（readyz 关键依赖 down/recovered；DLQ 耗尽） |
| 标注 API 契约测试 | ✅ 幂等覆盖断言 |

## 下轮输入

Phase 4 DoD 复核（标准全部 ✅ 后打 v0.5.0）→ structlog 结构化日志接入评估 → Phase 5（RAGAS 评估闭环、爬虫增量、压测）启动。遗留登记：M-1（SSRF rebinding TOCTOU 自定义 transport）、M-4（transformers/gradio CVE）、M-6（scrypt 档位——Web 口令已用 Argon2id，API Key 段 Phase 5 提档）。
