# 路线对齐审计 · 2026-08-14 · 第 19 轮（Phase 2 · 混合检索）

## 本轮完成

- **RRF 纯函数**：手算测试 5 例（含重复 id 去重后位次重排的语义修正）
- **纯 Python BM25**（ADR-004）：tokenizer（拉丁词 + CJK 字符）、IDF/Bm25Hit 手算验证 5 例
- **HybridRetriever**：KB 级内存索引 + dense/sparse 各 top-50 + RRF 融合 + 缓存失效（摄取/删除触发）
- **关键缺陷修复**：切换混合检索后 RRF 分数量纲（~0.03）与拒答阈值（0.25，余弦量纲）错位——拒答判定改为 dense 语义相似度（审计 ARC-014 预警场景的实锤）；SearchResult 增加 dense_score 双分数语义
- ADR-003/ADR-004 批准落地；Qdrant 本地模式无 full-text 能力实测定案
- 191 测试全绿 · ruff/pyright 全零 · 评测基线门禁通过 · 自动提交推送（41e57e2）

## 五查结果

1. **范围** ✓ 严格 Phase 2 交付物（#18）
2. **技术** ✓ 零新增依赖
3. **架构一致性** ✓ 架构 §6 混合检索链路（dense+sparse→RRF→过滤→top-N）；拒答与 §7 对齐
4. **规范** ✓ 191 测试 · 覆盖率 93%+ · 全门禁
5. **交付物** ✓ rrf/bm25/hybrid 模块 + 测试 + 两 ADR

## 下轮输入

#19 重排层（reranker Protocol + GPU/CPU 降级路径 + top-8 截断）→ #22 检索回归（混合检索下重定基线，目标 recall@10 ≥0.95）
