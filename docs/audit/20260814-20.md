# 路线对齐审计 · 2026-08-14 · 第 20 轮（Phase 2 · 重排层）

## 本轮完成

- **Reranker Protocol**（core/retrieval/rerank.py）：NoopReranker（off 降级路径）/ CrossEncoderReranker（local，bge-reranker-v2-m3，pragma 标记 GPU 手动验证路径）
- **SearchService 接入**：混合检索 top-50 → 重排 → rerank_top_k（8）截断；main.py 从 Settings 接线
- **score 语义定稿**：score = 排序分（重排存在时为重排分数，否则 RRF 融合分）；dense_score 恒为语义相似度（拒答判定，ARC-014）
- 假重排排序/截断测试（审计 F6 手法）；197 测试全绿；自动提交 d987269

## 五查结果

1. **范围** ✓ #19 交付物
2. **技术** ✓ 零新增依赖（CrossEncoder 复用 embed extra）
3. **架构一致性** ✓ §6 重排 top-8 / §7 拒答判定语义
4. **规范** ✓ 197 测试 · ruff/pyright 全零 · 基线门禁通过
5. **交付物** ✓ 模块 + 工厂 + 测试 + 接线

## 下轮输入

#20 parent-child 回填与查询路由（含过滤优先于融合专项回归）→ #21 拒答三分支 → #22 检索回归重定基线（混合检索下 bge-m3 实测）
