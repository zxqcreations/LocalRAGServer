# 路线对齐审计 · 2026-08-15 · 第 14 轮（CI 门禁修复 + 遗留清单修正）

## 本轮完成

- **CI 门禁修复**（GitHub Actions 三连红 → 全绿，两轮定位）：
  - 第一轮：CI `uv sync --extra dev` 装不到 `mcp`/`argon2-cffi`（核心代码导入但未声明）→ pyright reportMissingImports 连锁报错，ubuntu/windows 双挂。修复：主依赖补 `mcp>=1.2`、`argon2-cffi>=23.1`；显式声明 `sqlalchemy>=2.0`（registry/迁移直接导入）；`[tool.pyright] exclude scripts/spike`（GPU 实测工具，torch 走 pip 不进 lock）；uv.lock 更新（torch 不入 lock，本机 GPU 环境安全）
  - 第二轮：windows job 冒烟脚本崩溃——**Windows 控制台 cp1252 代码页写中文抛 UnicodeEncodeError**（目标平台真实缺陷，非 CI 特有）。修复：`configure_logging` 就地 reconfigure 流为 UTF-8（errors=replace）；用 reconfigure 而非新建 TextIOWrapper（包装会劫持 pytest 捕获流 buffer 生命周期，本地实测 teardown 崩溃后改进）；smoke.py 同样处理；单测 +2
  - 最终：5 job 全 success（ubuntu/windows 门禁 + Web 构建 + 评测回归；GPU 手动触发 skip）
  - 导入面审计：core/apps/tests/scripts 全部顶层导入与声明集比对，仅 sqlalchemy 一项漏声明（已补）
- **遗留清单修正**（安全审查后待办复核）：
  - M-1（SSRF rebinding TOCTOU）：**已实现并测试**——UrlFetcher 固定已解析 IP 连接（Host/SNI 保持原域名）+ 逐跳重校验，tests/test_ssrf.py 两用例覆盖。移出遗留。
  - M-4（transformers/gradio CVE）：**已闭环**——transformers 4.57.6 / gradio 6.8.0 / mineru 3.4.5，pip-audit 无漏洞报告。移出遗留。
  - M-6（scrypt 档位）：**评估闭环**——API key 为服务端签发的高熵随机串（token_urlsafe），无离线爆破面；scrypt N=2^14（r=8，≈16MB/次）已是每请求在线验证的合理上限，提档会放大限流并发下的内存峰值。维持现状，决策记录在案。

## Phase 4 DoD 复核（quality.md §2）

| 退出标准 | 证据 | 结论 |
|---|---|---|
| Web 管理端独立认证（强制改密/HttpOnly/CSRF/RBAC） | 契约测试 10 例；CSRF 独立 token（H-1 修复） | ✅ |
| 5 模块 Playwright E2E | 4/4 绿 × 2 轮可重复（含断点 320px、刷新回归） | ✅ |
| trace_id 全链路验证 | 中间件 + 测试 | ✅ |
| 告警规则集上线 | readyz 边沿触发 + DLQ 耗尽 | ✅ |
| 标注 API 契约测试 | 幂等覆盖断言 | ✅ |

**DoD 通过 → v0.5.0**（CI 验证绿后打 tag）。

## 门禁记录

| 门禁 | 结果 |
|---|---|
| pytest 276 例 / 覆盖率 | ✅ 93.42% |
| ruff / pyright / bandit / pip-audit | ✅ 全零 |
| E2E 4 用例 | ✅ 全绿 |
| vite build / 冒烟 | ✅ |
| GitHub CI（ubuntu + windows 双平台） | ✅ 全绿（run 31862696575） |

## 发布记录

**v0.5.0**（Phase 4 DoD 通过）：Web 管理端五模块 + 独立认证 + 标注 API + E2E + 可观测性告警。
附加交付：`docs/design/structlog-integration.md`（接入计划定稿，Phase 5 首批实现）。

## 下轮输入

Phase 5 启动：structlog 接入落地（设计已定稿：trace_id contextvars + 最小埋点清单）→ RAGAS 评估闭环（本地 llama.cpp 后端）→ 爬虫增量摄取 → 压测基线。
