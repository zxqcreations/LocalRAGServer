# 路线对齐审计 · 2026-08-15 · 第 02 轮（Phase 2 · 拒答策略）

## 本轮完成

- **should_refuse 纯函数**（core/generation/refusal.py）：best < threshold 拒答语义；0.249/0.250/0.251 三分支参数化测试（审计 F12）
- **分数字段按重排激活选择**（refusal_field）：off → dense_score（余弦量纲）；激活 → score（cross-encoder）——量纲解耦落地（ARC-014）
- **hard-negative 用例**（F12.4）：dense 高分 + rerank 低分 → 拒答
- chat 路由接线；212 测试全绿（+6）；自动提交 2987080

## 五查结果

1. **范围** ✓ #21 交付物
2. **技术** ✓ 纯函数抽象，零依赖
3. **架构一致性** ✓ §7 拒答策略；ARC-014 量纲解耦
4. **规范** ✓ 212 测试 · ruff/pyright 全零
5. **交付物** ✓ 模块 + 接线 + 测试

## 下轮输入

#22 检索质量回归与快照：混合检索下重定基线（bge-m3 dense 实测目标 recall@10 ≥0.95）、golden 向量/重排快照（tests/golden/）
