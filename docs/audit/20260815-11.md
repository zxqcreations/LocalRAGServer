# 路线对齐审计 · 2026-08-15 · 第 11 轮（Phase 4 · 管理端认证）

## 本轮完成

- **Argon2id 口令**（core/security/admin.py）：人类口令专用档（M-6）；会话只存 SHA-256 哈希；
  初始密码一次性生成（data_dir/admin_initial_password + 日志）
- **AdminUser/AdminSession 表**：RBAC 两档；must_change_password 强制改密流；会话 30 分钟 TTL
- **/admin/api/***：login/change-password/logout/me + KB 列表/指标快照/审计/Key 签发（明文仅一次）/吊销
- **管理中间件**：通道隔离（API Key 显式 403）/Cookie（HttpOnly+SameSite=Lax）/
  CSRF（登录响应下发 token，X-CSRF-Token 头回传）/登录限流（10/分钟 per-IP）
- **契约测试 7 例全过**：登录流程/Cookie 属性/强制改密/CSRF 拒绝/通道隔离/RBAC 越权/Key 签发吊销
- 272 测试全绿 · ruff/pyright 全零 · 自动提交 2cda185

## 本轮修复的中间件级缺陷（TDD 暴露）

1. 中间件洋葱顺序：admin 放行后 auth 仍拦截 /admin/ → auth 显式跳过管理路径
2. login 免会话分支提前 return 跳过 Cookie 下发 → 抽公共后处理
3. SQLite naive 时区比较（同 M-5 模式）→ 统一 naive UTC 口径

## 五查结果

1. **范围** ✓ #27 交付物
2. **技术** ✓ argon2-cffi 入主依赖
3. **架构一致性** ✓ web-admin-auth.md 全契约
4. **规范** ✓ 272 测试 · 全门禁
5. **交付物** ✓ 核心 + 表 + 路由 + 中间件 + 契约测试

## 下轮输入

#29 Web 前端五模块（Vue3：KB 管理/检索调试台/Key 管理/系统监控/评估面板——调试台三阶段数据由 search 服务导出同一中间结构）+ 标注 API
