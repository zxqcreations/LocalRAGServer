# 路线对齐审计 · 2026-08-16 · 第 34 轮（v1.1 首项：MCP streamable HTTP）

## 本轮完成

**ADR-006 四项前置条件全部落地，streamable HTTP transport 上线（v1.1 首项）：**

1. **挂载点 + 请求级 ACL 注入**（apps/api/main.py）
   - `/_mcp` → `app.api_route("/mcp", GET/POST/DELETE)`，请求全权转发
     `StreamableHTTPSessionManager.handle_request`（json_response=True，
     ASGI 调用经 `request._send` 原路写回，端点返回 None）
   - `_mcp_allowed_kbs: ContextVar[AllowedKbs]` + `_mcp_actor` 两个 contextvar：
     auth_middleware（单一强制点）认证后写入，MCP 工具层经 lambda resolver 读取
     —— 与 REST 完全同源（F-13 语义不变）
   - 管理器 `run()`（anyio 任务组）随 lifespan 启停
2. **`allow_local_paths=False`**：HTTP 通道拒绝本地文件摄取（H-2 延续），
   stdio 通道保持 True（mcp_stdio.py 不受影响）
3. **TLS 前置**：部署侧要求（phase6-plan 附录 A 第 6 项），代码层默认回环绑定
4. **越权矩阵测试**（tests/test_mcp_http.py，3 例）：真实 HTTP 通道验证
   401 / 跨库检索拒绝 / M-7 跨库任务探测拒绝 / 本库放行 / 本地路径拒绝 /
   列表过滤 / 审计落库（actor + trace_id）

**附带完成：**

- **MCP 工具调用审计**：`mcp_*` 独立动作码（list_kbs/search/ask/ingest/status），
  actor 经 contextvar 注入（Key id / master / mcp:stdio），trace_id 取 structlog
  contextvars——trace 中间件路径覆盖扩展至 `/mcp`
- **M-7**：`get_document_status` 校验任务归属 KB 在调用方 ACL 内
  （跨库 job_id 探测显式「无权限」，与 REST 403 同语义）
- **文档同步**（ADR-006 后果条款）：architecture.md §9.2/§1/技术栈表修正为
  v1.1 已实现（并修正工具参数表与实现对齐）；user-guide.md 新增 §5.5
  MCP 接入（stdio 配置 + HTTP 会话流程 curl 示例 + 通道语义差异）

## 审查记录

code-reviewer（APPROVE：0C/0H/3M/3L）+ security-reviewer（WARN：1H/3M/2L）双审
均以实测方式验证（真实 uvicorn 下的 ASGI 消息序、跨 Key 会话恢复探针、任务级
contextvar 记录），核心 ACL 不变量（逐请求、deny-by-default、显式 403）确认完好。
全部发现本轮修复：

| 发现 | 处置 |
|---|---|
| **H-1 会话归属未绑定**（任何持 Key 者可恢复/终止他人会话） | auth_middleware 认证后写 `scope["user"]`（AuthenticatedUser，client_id=Key id/master，token 字段留空）→ SDK 跨 Key 恢复即拒（404 与「会话不存在」一致）；会话 ID 日志抑制至 WARNING；`session_idle_timeout=1800` 防僵尸会话堆积 |
| M 双响应依赖（端点返回 None + SDK 自发送，靠 BaseHTTPMiddleware 静默丢弃） | 改为原始 ASGI 挂载 `app.mount("/mcp", _mcp_asgi)`，响应仅由 SDK 发出 |
| M chunked 请求绕过 Content-Length 上限 | ASGI 层有界读体（按实际字节钳制 10MB，读完后单条重放给 SDK） |
| M 工具内部异常回显细节（LLM 端点/文件路径） | `_handle_call_tool` 增加兜底 `except Exception`：细节进服务端日志，客户端仅通用文案；stdlib 通道日志改走 stderr（stdout 是协议通道） |
| L 默认 "*" 潜在 fail-open | `_mcp_armed` 哨兵 contextvar：仅认证中间件置位，挂载处未置位即 500 |
| L 越权拒绝无审计 | `mcp_*_denied` 动作码（kb 原文截断 64 防注入；get_document_status 记真实 job.kb_id） |
| L mcp_ingest 审计先于入队 | 移至 `enqueue_ingest` 之后（先做事后记账） |
| L M-7 拒绝暴露 job 存在性 | 保留（项目 F-13 显式 403 惯例）；SDK 会话层已用「不可区分」语义 |
| 测试缺口（跨 Key 恢复/会话生命周期/413/拒绝审计） | 新增 3 例（test_mcp_http.py 共 6 例 HTTP 通道测试） |

## 门禁记录

| 门禁 | 结果 |
|---|---|
| pytest（353 例） | ✅ 90.48% 覆盖（MCP HTTP 6 例 + stdio 7 例） |
| ruff / pyright / bandit | ✅ 全零（B106 nosec：AccessToken token 字段刻意留空） |
| CI | 提交后验证 |

## 下轮输入

- 观察期到期待复核（回滚触发条件：P95 退化 >30%）
- v1.1 余项：限流 Redis 化（ADR-005 生产路径）、BM25 稀疏检索结构优化
- 登记项：浏览器 MCP 客户端无 CORS（与 API Key 认证设计一致，面向非浏览器 Agent）
