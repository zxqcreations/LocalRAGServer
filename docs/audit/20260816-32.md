# 路线对齐审计 · 2026-08-16 · 第 32 轮（模型清理 + SSRF 收敛）

## 本轮完成

- **旧模型清理脚本**（49aab19）：prune_old_models.py（保留 ≥72h 契约执行侧，
  未被 MANIFEST 引用 + 超期才列候选，--apply 删除）
- **MANIFEST 入版本库**（重要修复）：gitignore `models/` 例外陷阱导致
  MANIFEST.json 从未被追踪——改为 `models/*` 例外生效，补入版本库
  （含 judge 模型条目）
- **SSRF L 级收敛**（fec0e52）：L-1 非法端口转防护错误（原 500）；
  L-2 白名单单段条目（公共后缀）不做后缀匹配（防任意域放行退化）
- 附带：孤儿 llama-server 进程清理（GPU 释放）

## 门禁记录

| 门禁 | 结果 |
|---|---|
| 相关测试（ssrf/auth/api） | ✅ 全绿 |
| ruff / pyright / bandit | ✅ 全零 |
| CI | ✅（35a8148 绿；fec0e52 验证中） |

## 下轮输入

L 级余项继续（L-3/L-4/L-5/L-6/L-8/L-9）→ v1.1 功能项启动
（MCP HTTP transport 为 ADR-006 既定首项）。
观察期 ~明晨 02:00 到期，期间监控持续。
