# 路线对齐审计 · 2026-08-16 · 第 31 轮（v1.0 后打磨：M-9/M-11/测试基座）

## 本轮完成

- **M-9 会话清扫**（7abce66）：Registry 启动清扫过期会话
  （AdminSession 只增不删残余风险）；审计保留策略文档化不自动删
- **M-11 依赖 CVE 监控补偿**：requirements-security.txt（pip 直管包清单）
  → CI 专用 pip-audit job；web-build 加 npm audit
- **conftest 双重加载根治**（35a8148）：统一 `from conftest import`
  （pytest 顶层模块实例），消除命名空间包双加载的隐藏债务
- 附带修复：gitignore `/storage/` 锚定根目录（原规则误伤 core/storage/）
- 文档体系：user-guide.md 完整手册 + README 更新 + 4 处过时标注修复

## 门禁记录

| 门禁 | 结果 |
|---|---|
| pytest（344 例） | ✅ 90.66% 覆盖 |
| ruff / pyright | ✅ 全零 |
| CI | ✅（5f764a5 绿；35a8148 验证中） |

## 下轮输入

剩余任务按序：prune_old_models.py（#15）→ L 级余项（#16）→
v1.1 功能（MCP HTTP transport / 限流 Redis / sparse 优化）。
观察期 ~明晨 02:00 到期。
