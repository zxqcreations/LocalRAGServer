# 路线对齐审计 · 2026-08-14 · 第 06 轮

## 本轮目标

P0-2 硬件兼容性 Spike（Turing SM75）——P0 行动清单最后一项

## 实际完成

- **生成栈实测**：llama.cpp b10427（CUDA 12.4 钉版）+ Qwen3-8B Q4_K_M（5.2GB 经注册表代理下载完成）：**Prompt 74.0 t/s · 解码 29.7 t/s**，显存 ~1.8GB，思考模式输出正常
- **嵌入栈实测**：torch 2.9.1+cu126 在 Turing 上 CUDA 可用；bge-m3 GPU **141 条/s**（CPU 5 条/s）、驻留 ≈888MB
- **交付物**：docs/spike/sm75-matrix.md（兼容矩阵 + 环境约束 + 已知问题）+ docs/adr/001-generation-backend.md（llama.cpp MVP / vLLM 钉版生产 / TEI 留待 Linux + 四条不可妥协约束）
- 架构 §8.4 吞吐改为实测口径；quality.md P0-2 状态更新

## 本轮发现的环境级问题（已记录）

1. **uv 0.11.13 不读 pyproject 的 `[[tool.uv.index]]`/`[tool.uv.sources]`**（`--index` 把索引名当路径）→ GPU torch 走 `uv pip install --index` 工作区方案，Phase 1 深查
2. Windows PyPI 默认 torch 为 +cpu（审计 AUD-14 判断已过时）
3. llama-cli stdin EOF 下交互模式死循环（16 万行提示符）→ 必须 `-st/--single-turn` 或 llama-server
4. 后台 GPU 长任务在会话轮转时被终止 → 实测拆小、落盘日志

## 五查结果

1. **范围** ✓ 无蔓延（Spike 即 P0-2 计划项）
2. **技术** ✓ 选型经实测支撑；ADR-001 正式生效
3. **架构一致性** ✓ §8.4 已改实测口径；§4 Profile D 验证成立（生成+嵌入 ≈2.7GB/11GB，余量充足）
4. **规范** ✓ 80 测试全绿 · ruff 全过
5. **交付物** ✓ P0-1..P0-9 全部落地，Spike 交付物齐备

## Phase 0 收尾待办（下轮输入）

1. README 补充 Windows GPU 嵌入安装指引（torch cu126 工作区方案，审计 AUD-14）
2. CI 流水线文件（P0-5：.github/workflows/ci.yml 三层，可先落文件，推送后生效）
3. git 初始提交——**需用户授权**（用户规范：提交需明确要求）
4. 冒烟脚本 + 五门禁写入 CI 门禁清单；打 tag v0.1.0 前复核 Phase 0 DoD 清单（quality.md §2）
