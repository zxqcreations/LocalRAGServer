# 路线对齐审计 · 2026-08-15 · 第 27 轮（门③ SLO 压测首测）

## 本轮完成

- **万级 SLO 压测**（f308a4d）：bench_search --fast 批量入库 1 万文档 +
  四段耗时分解（dense/sparse/fuse/fetch）
- **首测结果**：P95 **749.3ms 超限**（目标 <500ms）；分解：
  dense=194.2 · sparse=84.0 · fuse=272.1 · fetch=1.7
- **根因定位**：Qdrant local 模式 brute-force 检索（无 HNSW 索引，
  1 万点精确计算 ~200ms/查询）——local 模式是开发形态，
  生产形态为 Qdrant server（HNSW）

## 门禁记录

| 门禁 | 结果 |
|---|---|
| ruff / pyright / bandit | ✅ 全零 |
| CI | ✅（2d419fa 绿；f308a4d 验证中） |

## 下轮输入

门③ 闭环路径：下载 Qdrant Windows 二进制 → 本机启动 Qdrant server
（HNSW）→ 同规模复测（目标 P95<500ms 达标）→ 部署文档更新
（生产形态 qdrant server + 开发形态 local 模式边界标注）。
若 server 实测仍超限：进入优化决策（ADJ：sparse 段缓存 / dense 参数调优）。
