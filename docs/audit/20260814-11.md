# 路线对齐审计 · 2026-08-14 · 第 11 轮

## 本轮目标

#17 批量导入 CLI + 性能基准（Phase 1 质量门）

## 实际完成

- **批量导入核心**（core/ingest/importer.py）：目录递归扫描 + 幂等键 (kb_id, content_hash)（审计 ARC-018 语义：同 KB 去重、跨 KB 允许）+ source 落盘 + 入队统计（docs_per_hour）；3 例测试（基本/幂等重跑/跨 KB）
- **CLI**（scripts/import_docs.py）+ **吞吐基准脚本**（scripts/bench_ingest.py，报告落 docs/perf/）
- **真实数据基准**（100 篇领域 MD，18,893 chunks）：**2850 文档/小时 · 149.6 chunk/s**（stub 管线开销口径）→ 十万级文档 ≈35 小时单机（stub）；bge-m3 GPU 口径见 sm75-matrix（141 条/s），与架构"解析是瓶颈、须多 Worker"判断一致

## 五查结果

1. **范围** ✓ 无蔓延
2. **技术** ✓ 零新增依赖
3. **架构一致性** ✓ 幂等语义与审计 ARC-018 逐字一致；基准口径与 §8.4 对照
4. **规范** ✓ 173 测试全绿 · ruff/pyright 全零 · 覆盖率 93%+
5. **交付物** ✓ 导入核心/CLI/基准/报告

## 下轮输入

#12 收尾（MinerU 大 PDF 实测仍在跑）；#15 PG 迁移（需用户装 PG 或显式延期登记）；Phase 1 剩余退出标准复核（状态机全转移绿/断点续传已测/幂等已验证/吞吐数据已落 docs/perf/）
