# 路线对齐审计 · 2026-08-14 · 第 02 轮

## 本轮目标

P0-1（git init）+ P0-3（配置整改）+ P0-4 工具链部分（五道门禁落地）

## 实际完成

- **git 仓库**：项目根初始化（main 分支）；.gitignore 补 .coverage/htmlcov/models/*.log/.claude/settings.local.json（AUD-13）；未提交（等用户许可后做初始提交）
- **配置整改**（P0-3 全部落地，TDD：先写 test_config.py 6 例）：
  - 数据路径从 data_dir 单一来源派生（qdrant_path/database_url 可显式覆盖）
  - 未知 RAG_* 环境变量 fail-fast（显式扫描 os.environ——实测 pydantic-settings 的 extra=forbid 对 env 源无效，已换实现）
  - LLM 默认端点 vLLM 口径（9001 + Qwen3-8B-AWQ），不再指向不存在的 Ollama
  - 检索参数补齐：retrieval_top_k/rerank_top_k/rrf_k/HNSW/chunk 尺寸/embed batch/reranker 占位
  - openai 嵌入后端强制要求 API Key；未知后端 fail-fast
  - Python 版本统一钉 3.13（pyproject + ruff + 架构文档 §3 同步）
  - HNSW 参数接线进 QdrantVectorStore（HnswConfigDiff + SearchParams），chunk 参数接线进 SearchService
- **五道门禁全部落地并首次通过**：ruff（含 20 处自动修复 + 23 处手工修复，路由层全面改用 Annotated 依赖注入消除 16 处 B008）、pyright 0 错误（SQLModel 类型摩擦用文件级声明 + 定点 ignore 处理）、pytest 69 全绿、bandit 零告警、pip-audit 无漏洞

## 五查结果

1. **范围** ✓ 无蔓延
2. **技术** ✓ 选型未变；工具链补齐（pyright/bandit/pip-audit/pytest-asyncio 入 dev extra）
3. **架构一致性** ✓ P0-3 整改后 config 与架构 §4/§6/§8.2 对齐
4. **规范** ✓ 五门禁全绿；覆盖率 92%+（69 测试）
5. **交付物** ⚠️ 剩余：conftest 隔离基座、语义化 stub、最小认证、eval 种子、冒烟脚本、硬件 Spike

## 偏离项与处置

| 偏离 | 处置 |
|---|---|
| pydantic-settings extra=forbid 对 env 源无效（审计建议的机制不成立） | 换用显式 os.environ 扫描校验，测试固化（test_config.py） |
| pydantic typeshed 不支持 PEP 695 泛型（Envelope[T] pyright 报错） | 回退 Generic[T] + noqa: UP046 注释说明 |
| SQLModel 列表达式与 pyright 类型推断冲突 | 文件级 reportArgumentType=false + 文档化说明（registry.py 头部） |
| git 初始提交 | 待用户许可（用户规范：提交需明确授权） |

## 下轮输入

#8 剩余：conftest autouse 隔离 fixture + stub 升级 n-gram 语义化 + 冒烟脚本 → #9 最小认证与上传防护 → #10 eval 种子 → #11 硬件 Spike（模型下载走代理，后台不设超时）
