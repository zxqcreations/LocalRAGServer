# 路线对齐审计 · 2026-08-14 · 第 09 轮

## 本轮目标

#14 Celery 异步摄取管线：状态机实现 + 任务链 + 契约落地

## 实际完成

- **状态机**（core/ingest/state.py）：七状态 + 合法转移白名单 + 恢复转移 + retry_from + stage_reached；49 对全矩阵表驱动测试
- **注册表**：IngestJob 表（stage/attempt/failed_at/error）、transition_job（同状态幂等 no-op、非法转移拒绝、failed_at 记录、Document.status 同步）
- **管线**（core/ingest/pipeline.py）：四阶段幂等执行 + **产物哈希链**（source→text→chunks→vecs 每级校验上游变更，防中间产物陈旧）+ run() 失败即转 FAILED
- **任务链**（core/ingest/tasks.py）：parse/chunk/embed/index 四任务 + 链式编排（.si 不可变签名）+ 失败重试（attempt 上限 5 进 DLQ 语义）
- **对账**：VectorStore.count 接口（内存/Qdrant 双实现），indexed 后核对点数
- 契约 v1.1 两次修订：FAILED 恢复转移、同状态 no-op（docs/design/ingest-state-machine.md 同步）

## 本轮修复的真实缺陷（TDD 暴露）

1. retry_from 缺 UPLOADED 键（KeyError 级联出 failed→failed 非法转移）
2. 恢复后阶段方法重入触发同状态转移拒绝 → 契约增补 no-op 语义
3. 中间产物陈旧：source 变更后 text.txt 未失效 → 引入哈希链校验
4. 对账时序：契约 §2.5 要求 indexed 后核对，实现先核对后转移 → 修正顺序

## 五查结果

1. **范围** ✓ 无蔓延
2. **技术** ✓ celery 入固定栈（架构 §13 既定）
3. **架构一致性** ✓ 状态机逐字遵循契约；ingest_jobs 表落地
4. **规范** ✓ 143 测试全绿（+59）· 覆盖率 93.13% · ruff/pyright/bandit 全零 · 冒烟/评测回归通过
5. **交付物** ✓ 状态机 + 管线 + 任务链 + 契约文档 + 测试

## 下轮输入

#12 收尾（MinerU 大 PDF 实测进行中）；#16 URL 摄取 + SSRF 防护（与上传 API 异步化同批）；#17 批量导入 CLI
